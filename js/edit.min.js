$.glue.canvas=function(){return{update:function(b){if(b===undefined){b=$(".object")}var c=0;var a=0;$(b).each(function(){var d=$(this).position();if(c<d.left+$(this).outerWidth()){c=d.left+$(this).outerWidth()}if(a<d.top+$(this).outerHeight()){a=d.top+$(this).outerHeight()}});if(c<$(window).width()){c=$(window).width()}if(a<$(window).height()){a=$(window).height()}$("body").css("width",c+"px");$("body").css("height",a+"px");$.glue.grid.update()}}}();$.glue.color=function(){var e=function(h){h=$.trim(String(h)).toLowerCase();var g;if(g=/^#([0-9a-f])([0-9a-f])([0-9a-f])$/.exec(h)){return{r:parseInt(g[1]+g[1],16),g:parseInt(g[2]+g[2],16),b:parseInt(g[3]+g[3],16),a:1}}if(g=/^#([0-9a-f]{2})([0-9a-f]{2})([0-9a-f]{2})$/.exec(h)){return{r:parseInt(g[1],16),g:parseInt(g[2],16),b:parseInt(g[3],16),a:1}}if(g=/^rgba?\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)\s*(?:,\s*([0-9.]+)\s*)?\)$/.exec(h)){return{r:parseInt(g[1],10),g:parseInt(g[2],10),b:parseInt(g[3],10),a:g[4]!==undefined?parseFloat(g[4]):1}}if(h=="transparent"){return{r:0,g:0,b:0,a:0}}return false};var d=function(g){g=Math.max(0,Math.min(255,Math.round(g)));return(g<16?"0":"")+g.toString(16)};var c=function(g){return"#"+d(g.r)+d(g.g)+d(g.b)};return{parse:e,to_hex:c,complementary:function(g){var h=e(g);if(!h){return g}return c({r:255-h.r,g:255-h.g,b:255-h.b})},average:function(h,i){var g=e(h);var j=e(i);if(!g||!j){return h}return c({r:(g.r+j.r)/2,g:(g.g+j.g)/2,b:(g.b+j.b)/2})}}}();$.glue.colorpicker=function(){var e=false;var f=false;var d=false;var a=false;var c=$('<input type="color" id="glue-colorpicker" class="glue-ui">');$(c).bind("input",function(g){if(typeof e=="function"){e($(c).val())}});$(c).bind("change",function(g){$.glue.colorpicker.hide()});var b=function(g){if(!$(g.target).hasClass("glue-ui")&&$(g.target).parents(".glue-ui").length==0){$.glue.colorpicker.hide();g.stopImmediatePropagation()}};return{hide:function(g){if(a){if(!d&&(g===undefined||g==false)){d=true;if(typeof f=="function"){f($(c).val())}}$(c).detach();a=false}$("body").unbind("click",b)},is_shown:function(){return a},set_color:function(g){var h=$.glue.color.parse(g);var i=h?$.glue.color.to_hex(h):"#ff0000";if(h&&h.a==0){i="#ffffff"}$(c).val(i)},show:function(i,h,j,g){if(a){$.glue.colorpicker.hide()}e=j;f=g;d=false;if(typeof i!="string"||i.length==0){$(c).val("#ff0000")}else{$.glue.colorpicker.set_color(i)}$("body").append(c);a=true;$("body").bind("click",b);c.get(0).click()}}}();$.glue.contextmenu=function(){var e=10;var g=[];var c={};var b=false;var d=false;var f=[];var a={};$(".object").live("glue-deselect",function(h){$.glue.contextmenu.hide()});$(".object").live("glue-movestart",function(h){if(this==b){d=b;$.glue.contextmenu.hide()}});$(".object").live("glue-movestop",function(h){if(this==d){$.glue.contextmenu.show(d);d=false}});$(".object").live("glue-select",function(h){if($(".glue-selected").length==1){$.glue.contextmenu.show(this)}else{$.glue.contextmenu.hide()}});return{hide:function(){if(b){while(g.length){var h=g.shift();$(h.elem).trigger("glue-menu-deactivate");$(h.elem).detach()}while(f.length){var h=f.shift();$(h.elem).trigger("glue-menu-deactivate");$(h.elem).detach()}b=false}},is_shown:function(){if(b){return true}else{return false}},register:function(h,i,j,k){if(!c[h]){c[h]=[]}if(k===undefined){k=e}c[h].push({name:i,elem:j,prio:k})},reuse:function(j,k,h,o){if(o===undefined){o=e}for(var n in c){for(var l=0;l<c[n].length;l++){if(c[n][l].name==k){var m=$(c[n][l].elem).clone(true);$.glue.contextmenu.register(j,h,m,o);return m}}}return false},show:function(r){if(b){if(r==b){return}else{$.glue.contextmenu.hide()}}if(!$(r).hasClass("locked")){for(var y in c){if($(r).hasClass(y)){var t;if(y=="object"){t=g}else{t=f}for(var s=0;s<c[y].length;s++){var x=false;for(var p=0;p<t.length;p++){if(c[y][s].prio<t[p].prio){t.splice(p,0,c[y][s]);x=true;break}}if(!x){t.push(c[y][s])}}}}}else{var t=g;for(var s=0;s<c.object.length;s++){if(c.object[s]["name"]=="object-lock"){t.push(c.object[s])}}}var m=$(r).attr("class").replace(/\s+/," ").split(" ");for(var y in a){for(var s=0;s<m.length;s++){if(y==m[s]){for(var p=0;p<a[y].length;p++){for(var n=0;n<g.length;n++){if(g[n].name==a[y][p]){g.splice(n,1);n--}}for(var n=0;n<f.length;n++){if(f[n].name==a[y][p]){f.splice(n,1);n--}}}}}}for(var s=0;s<2;s++){var t;var w=$(r).position().left;var v=$(r).position().top;var o=48;if(s==0){t=f;if(g.length){if(w-$(g[0].elem).outerWidth(true)<0){w=$(g[0].elem).outerWidth(true)+o}}else{if(w-o<0){w=o}}}else{t=g;if(f.length){if(v-$(f[0].elem).outerHeight(true)<0){v=$(f[0].elem).outerHeight(true)}}else{if(v-o<0){v=o}}}for(var p=0;p<t.length;p++){$(t[p].elem).attr("id","glue-contextmenu-"+t[p].name);if(t==g){$(t[p].elem).addClass("glue-contextmenu-left")}else{$(t[p].elem).addClass("glue-contextmenu-top")}$(t[p].elem).addClass("glue-ui");$(t[p].elem).css("position","absolute");$(t[p].elem).css("visibility","hidden");$(t[p].elem).css("z-index","201");$("body").append(t[p].elem);if(t==f){$(t[p].elem).css("left",w+"px");var u=v-$(t[p].elem).outerHeight(true);if(u<0){u=0}$(t[p].elem).css("top",u+"px");var h=$(t[p].elem).outerWidth(true)}else{var q=w-$(t[p].elem).outerWidth(true);if(q<0){q=0}$(t[p].elem).css("left",q+"px");$(t[p].elem).css("top",v+"px");var l=$(t[p].elem).outerHeight(true)}$(t[p].elem).data("owner",r);$(t[p].elem).trigger("glue-menu-activate");if($(t[p].elem).css("display")=="none"){continue}if(t==g){v+=l}else{w+=h}$(t[p].elem).css("visibility","");$(t[p].elem).hide();$(t[p].elem).fadeIn(333)}}b=r;d=false;return true},veto:function(h,i){if(!a[h]){a[h]=[]}a[h].push(i)}}}();$.glue.grid=function(){var c=[];var i=[];var h=[];var k=[];var d=false;var j=0;var a=false;var g=50;var e=50;var f=function(){if((j&1)){if(d!==$(document).height()||a!==$(document).width()){b();d=$(document).height();a=$(document).width();var p="#ffffff";if($("body").css("background-color").length){var m=$.glue.color.parse($("body").css("background-color"));if(m&&m.a!=0){p=$.glue.color.to_hex(m)}}for(var l=g;l<=a;l+=g){var o=$("<div></div>");$(o).addClass("glue-grid-y");$(o).addClass("glue-grid");$(o).addClass("glue-ui");$(o).css("background-color",$.glue.color.complementary(p));$(o).css("height",d+"px");$(o).css("left",l+"px");$(o).css("position","absolute");$(o).css("top","0px");$(o).css("width","1px");$(o).css("z-index","200");$("body").append(o);k.push(o)}for(var q=e;q<=d;q+=e){var o=$("<div></div>");$(o).addClass("glue-grid-x");$(o).addClass("glue-grid");$(o).addClass("glue-ui");$(o).css("background-color",$.glue.color.complementary(p));$(o).css("height","1px");$(o).css("left","0px");$(o).css("position","absolute");$(o).css("top",q+"px");$(o).css("width",a+"px");$(o).css("z-index","200");$("body").append(o);k.push(o)}for(var n in i){var o=$("<div></div>");$(o).addClass("glue-guide-x");$(o).addClass("glue-guide");$(o).addClass("glue-ui");$(o).css("background-color",$.glue.color.average($.glue.color.complementary(p),p));$(o).css("height",d+"px");$(o).css("left",i[n]+"px");$(o).css("position","absolute");$(o).css("top","0px");$(o).css("width","1px");$(o).css("z-index","200");$("body").append(o);c.push(o)}for(var n in h){var o=$("<div></div>");$(o).addClass("glue-guide-y");$(o).addClass("glue-guide");$(o).addClass("glue-ui");$(o).css("background-color",$.glue.color.average($.glue.color.complementary(p),p));$(o).css("height","1px");$(o).css("left","0px");$(o).css("position","absolute");$(o).css("top",h[n]+"px");$(o).css("width",a+"px");$(o).css("z-index","200");$("body").append(o);c.push(o)}}}else{b()}};var b=function(){while(k.length){var m=k.shift();$(m).remove()}while(c.length){var l=c.shift();$(l).remove()}d=false;a=false};return{add_guide_x:function(l){i.push(l)},add_guide_y:function(l){h.push(l)},mode:function(l){if(l===undefined){return j}else{j=l}},update:function(l){if(l!==undefined&&l){d=false;a=false}f()},x:function(l){if(l===undefined){return g}else{g=l}},y:function(l){if(l===undefined){return e}else{e=l}}}}();$.glue.menu=function(){var e=10;var f=false;var a={};var d="";var c=false;var b=function(g){if(!$(g.target).hasClass("glue-ui")&&$(g.target).parents(".glue-ui").length==0){$.glue.menu.hide(false)}};$(".object").live("glue-select",function(g){if(f){$.glue.menu.hide()}});return{hide:function(h){if(h===undefined||h){d=""}if(f){for(var g=0;g<f.length;g++){$(f[g].elem).trigger("glue-menu-deactivate");$(f[g].elem).detach()}f=false}$("body").unbind("click",b)},is_shown:function(g){if(g===undefined){if(f){return true}else{return false}}else{if(a[g]&&a[g]==f){return true}else{return false}}},prev_menu:function(){var g=d;d="";return g},register:function(k,j,l){if(!a[k]){a[k]=[]}if(l===undefined){l=e}var h=false;for(var g=0;g<a[k].length;g++){if(l<a[k][g].prio){a[k].splice(g,0,{elem:j,prio:l});h=true;break}}if(!h){a[k].push({elem:j,prio:l})}},show:function(j,p,n){if(!a[j]){return false}if(f){$.glue.menu.hide()}if(p===undefined){p=$(window).width()/2}if(n===undefined){n=$(window).height()/2}var g=0;var o=0;f=a[j];num_shown=0;for(var l=0;l<f.length;l++){var k=f[l].elem;$(k).addClass("glue-menu-"+j);$(k).addClass("glue-menu");$(k).addClass("glue-ui");$(k).css("left",p+"px");$(k).css("position","fixed");$(k).css("top",n+"px");$(k).css("visibility","hidden");$(k).css("z-index","201");$("body").append(k);$(k).trigger("glue-menu-activate");if($(k).css("display")=="none"){continue}else{num_shown++}if(g<$(k).outerWidth(true)){g=$(k).outerWidth(true)}if(o<$(k).outerHeight(true)){o=$(k).outerHeight(true)}}var r=1;while(r*r<num_shown){r++}var m=r;if(num_shown<=r*(r-1)){m--}var q=0;var h=0;for(var l=0;l<f.length;l++){var k=f[l].elem;if($(k).css("display")=="none"){continue}if(h==m){q++;h=0}$(k).css("opacity","0.0");$(k).css("visibility","");$(k).animate({left:(p-(r*g)/2+h*g)+"px",opacity:1,top:(n-(r*o)/2+q*o)+"px"},200);h++}$("body").bind("click",b);d=j;c={x:$(document).scrollLeft()+p,y:$(document).scrollTop()+n};return true},spawn_coords:function(){return c}}}();$.glue.object = function()
{
	var alter_pre_save = {};
	var reg_objs = {};
	// keyed by raw DOM element rather than jQuery's .data(), since jQuery
	// 1.5.2's clone() (used by save() on every save) hangs when an element
	// carries a .data() entry pointing to a Moveable instance - its internal
	// object graph is large/circular enough to blow up clone()'s data-cache
	// fixup walk
	var moveables = new WeakMap();

	$(document).ready(function() {
		$.glue.object.register_alter_pre_save('glue-selected', function(obj, orig) {
			var border = $(orig).outerHeight()-$(orig).innerHeight();
			var p = $(orig).position();
			// remove class
			$(obj).removeClass('glue-selected');
			// and remove border offset
			$(obj).css('left', (p.left+border/2)+'px');
			$(obj).css('top', (p.top+border/2)+'px');
			//$(obj).css('width', ($(orig).width()+border)+'px');
			//$(obj).css('height', ($(orig).height()+border)+'px');
		});
	});

	return {
		// obj .. element
		register: function(obj) {
			// prevent double registration
			if (reg_objs[$(obj).attr('id')]) {
				return false;
			} else {
				reg_objs[$(obj).attr('id')] = true;
			}
			// make sure everything has a z-index
			if (isNaN(parseInt($(obj).css('z-index')))) {
				$(obj).css('z-index', $.glue.stack.default_z());
			}

			var resizable = $(obj).hasClass('resizable');
			var m = new Moveable(document.body, {
				target: obj,
				container: document.body,
				draggable: true,
				resizable: resizable,
				// jQuery UI's resizable() only exposed e/s/se handles by default
				renderDirections: resizable ? ['e', 's', 'se'] : [],
				keepRatio: false,
				edge: false,
				origin: false,
				// mirror jQuery UI draggable's implicit viewport-edge auto-scroll
				scrollable: true,
				scrollContainer: document.documentElement,
				scrollThreshold: 40,
				scrollThrottleTime: 30
			});
			moveables.set($(obj).get(0), m);
			// Moveable's control box (drag/resize handles) stays
			// visibility:hidden - and thus unclickable - until a second
			// internal render pass completes; without user interaction that
			// never happens on its own, so it's kicked off explicitly here
			m.updateRect();

			// jQuery UI's draggable had a 10px distance threshold before a
			// mousedown turned into an actual drag (so a plain click doesn't
			// nudge the object) - Moveable has no equivalent, so it is
			// hand-rolled here
			var drag_started = false;
			var drag_axis = false;				// false, 'x' or 'y'
			var drag_orig_left = 0;
			var drag_orig_top = 0;
			var drag_mouse_start_x = 0;
			var drag_mouse_start_y = 0;
			var drag_multi_prev_left = 0;
			var drag_multi_prev_top = 0;

			m.on('dragStart', function(e) {
				drag_started = false;
				drag_axis = false;
				drag_orig_left = $(obj).position().left;
				drag_orig_top = $(obj).position().top;
				drag_mouse_start_x = e.clientX;
				drag_mouse_start_y = e.clientY;
			}).on('drag', function(e) {
				if (!drag_started) {
					if (Math.max(Math.abs(e.clientX-drag_mouse_start_x), Math.abs(e.clientY-drag_mouse_start_y)) < 10) {
						return;
					}
					drag_started = true;
					if ($('.glue-selected').length > 1 && $(obj).hasClass('glue-selected')) {
						drag_multi_prev_left = drag_orig_left;
						drag_multi_prev_top = drag_orig_top;
						$('.glue-selected').trigger('glue-movestart');
					} else {
						$(obj).trigger('glue-movestart');
					}
				}

				var left = e.left;
				var top = e.top;

				// constrain to axis when dragging with shift key pressed
				if (e.inputEvent.shiftKey) {
					if (!drag_axis) {
						drag_axis = Math.abs(e.clientX-drag_mouse_start_x) < Math.abs(e.clientY-drag_mouse_start_y) ? 'y' : 'x';
					} else {
						var diff = Math.abs(Math.abs(e.clientX-drag_mouse_start_x)-Math.abs(e.clientY-drag_mouse_start_y));
						if (50 < diff) {
							drag_axis = Math.abs(e.clientX-drag_mouse_start_x) < Math.abs(e.clientY-drag_mouse_start_y) ? 'y' : 'x';
						}
					}
					if (drag_axis == 'x') {
						top = drag_orig_top;
					} else {
						left = drag_orig_left;
					}
				} else {
					drag_axis = false;
				}

				// ignore grid when ctrl is pressed
				if (!e.inputEvent.ctrlKey && ($.glue.grid.mode() & 2)) {
					left = Math.round(left/$.glue.grid.x())*$.glue.grid.x();
					top = Math.round(top/$.glue.grid.y())*$.glue.grid.y();
				}

				$(obj).css('left', left+'px');
				$(obj).css('top', top+'px');

				if ($('.glue-selected').length > 1 && $(obj).hasClass('glue-selected')) {
					// dragging multiple selected objects
					var delta_left = left-drag_multi_prev_left;
					var delta_top = top-drag_multi_prev_top;
					$('.glue-selected').not(obj).each(function() {
						var p = $(this).position();
						$(this).css('left', (p.left+delta_left)+'px');
						$(this).css('top', (p.top+delta_top)+'px');
					});
					drag_multi_prev_left = left;
					drag_multi_prev_top = top;
				}
			}).on('dragEnd', function(e) {
				if (!drag_started) {
					return;
				}
				if ($('.glue-selected').length > 1 && $(obj).hasClass('glue-selected')) {
					$('.glue-selected').trigger('glue-movestop');
				} else {
					$(obj).trigger('glue-movestop');
				}
			}).on('scroll', function(e) {
				e.scrollContainer.scrollBy(e.direction[0]*15, e.direction[1]*15);
			});

			if (resizable) {
				m.on('resizeStart', function(e) {
					$(obj).trigger('glue-resizestart');
				}).on('resize', function(e) {
					var width = e.width;
					var height = e.height;
					// ignore grid when ctrl is pressed
					if (!e.inputEvent.ctrlKey && ($.glue.grid.mode() & 4)) {
						width = Math.round(width/$.glue.grid.x())*$.glue.grid.x();
						height = Math.round(height/$.glue.grid.y())*$.glue.grid.y();
					}
					$(obj).css('width', width+'px');
					$(obj).css('height', height+'px');
					$(obj).css('left', e.drag.left+'px');
					$(obj).css('top', e.drag.top+'px');
					$(obj).trigger('glue-resize');
				}).on('resizeEnd', function(e) {
					$.glue.object.save(obj);
					$(obj).trigger('glue-resizestop');
					$.glue.canvas.update(obj);
				});
			}

			$(obj).trigger('glue-register');
			$.glue.canvas.update(obj);
		},
		register_alter_pre_save: function(cls, func) {
			alter_pre_save[cls] = func;
		},
		resizable_update_tooltip: function(obj) {
			// no-op: Moveable's resize handles are separate overlay elements
			// (not children of obj), so there is nothing to attach a title
			// to. Kept for backward compatibility with existing callers.
		},
		save: function(obj) {
			var elem = $(obj).clone();
			var elem_cls = $(elem).attr('class').replace(/\s+/, ' ').split(' ');
			for (var i=0; i < elem_cls.length; i++) {
				if (typeof alter_pre_save[elem_cls[i]] == 'function') {
					alter_pre_save[elem_cls[i]](elem, obj);
				}
			}
			// trim element content
			// necessary, otherwise we'd be sending \n\t back again
			$(elem).html($.trim($(elem).html()));
			// convert to string
			var html = $('<div></div>').html(elem).html();
			// DEBUG
			//console.log(html);
			$.glue.backend({ method: 'glue.save_state', 'html': html });
		},
		// obj .. element
		// returns the Moveable instance managing obj's drag/resize, or
		// undefined - used by modules (e.g. lock.js) that need to toggle
		// draggable/resizable directly
		moveable_of: function(obj) {
			return moveables.get($(obj).get(0));
		},
		unregister: function(obj) {
			var m = moveables.get($(obj).get(0));
			if (m) {
				m.destroy();
				moveables.delete($(obj).get(0));
			}
			$(obj).trigger('glue-unregister');
			// can't update canvas here as object to be deleted is still in the
			// dom
		}
	};
}();

$.glue.sel = function()
{
	var key_moving = false;
	
	// this could probably also be body
	$('html').bind('click', function(e) {
		if (e.target == $('body').get(0)) {
			if ($('.glue-selected').length) {
				// deselect when clicking on background
				$.glue.sel.none();
				// prevent the menu from firing
				e.stopImmediatePropagation();
			}
		}
	});
		
	$('html').bind('keydown', function(e) {
		if (e.which == 9) {
			// cycle through all objects with tab key
			if ($('.glue-selected').length < 2) {
				var next = false;
				if ($('.glue-selected').next('.object').length) {
					next = $('.glue-selected').next('.object');
				} else {
					next = $('.object').first();
				}
				if (next) {
					$.glue.sel.none();
					$.glue.sel.select(next);
					// scroll to the selected objects if not currently visible
					var window_min_x = $(document).scrollLeft();
					var window_max_x = window_min_x+$(window).width();
					var window_min_y = $(document).scrollTop();
					var window_max_y = window_min_y+$(window).height();
					var h = $(next).outerHeight();
					var p = $(next).position();
					var w = $(next).outerWidth();
					// fit the entire object on the screen
					// TODO (later): scroll a bit more up/left for the any 
					// context menu to fit in there too
					if (p.left < window_min_x) {
						$(document).scrollLeft(p.left);
					} else if (window_max_x < p.left+w) {
						$(document).scrollLeft(window_min_x+p.left+w-window_max_x);
					}
					if (p.top < window_min_y) {
						$(document).scrollTop(p.top);
					} else if (window_max_y < p.top+h) {
						$(document).scrollTop(window_min_y+p.top+h-window_max_y);
					}
				}
			}
			return false;
		} else if (33 == e.which && e.shiftKey && $('.glue-selected').length) {
			// shift+pageup: move objects to top of stack
			// we can't use ctrl+page{up,down} as this cycles through tabs
			// only prevent scrolling here
			return false;
		} else if (34 == e.which && e.shiftKey && $('.glue-selected').length) {
			// shift+pagedown: move objects to bottom of stack
			return false;
		} else if (37 <= e.which && e.which <= 40 && $('.glue-selected').length) {
			// move selected elements with arrow keys
			var add_x = 0;
			var add_y = 0;
			if (e.which == 38) {
				add_y = -1;
			} else if (e.which == 39) {
				add_x = 1;
			} else if (e.which == 40) {
				add_y = 1;
			} else if (e.which == 37) {
				add_x = -1;
			}
			// shift multiplier
			if (e.shiftKey) {
				// this depends on the grid size
				add_x *= $.glue.grid.x();
				add_y *= $.glue.grid.y();
			}
			$('.glue-selected').not('.locked').each(function() {
				var p = $(this).position();
				// prevent elements from going completely offscreen
				if (1 < p.left+add_x+$(this).outerWidth()) {
					$(this).css('left', (p.left+add_x)+'px');
				}
				if (1 < p.top+add_y+$(this).outerHeight()) {
					$(this).css('top', (p.top+add_y)+'px');
				}
			});
			// scroll window if neccessary
			// TODO (later): implement for moving multiple objects
			if ($('.glue-selected').length == 1) {
				var window_min_x = $(document).scrollLeft();
				var window_max_x = window_min_x+$(window).width();
				var window_min_y = $(document).scrollTop();
				var window_max_y = window_min_y+$(window).height();
				var elem = $('.glue-selected');
				var p = $(elem).position();
				var w = $(elem).outerWidth();
				var h = $(elem).outerHeight();
				if (p.left < window_min_x) {
					$(document).scrollLeft(p.left);
				} else if (window_max_x < p.left+w) {
					$(document).scrollLeft(p.left+w);
				}
				if (p.top < window_min_y) {
					$(document).scrollTop(p.top);
				} else if (window_max_y < p.top+h) {
					$(document).scrollTop(p.top+h);
				}
			}
			// trigger event (once, cleared in keyup)
			if (!key_moving) {
				$('.glue-selected').not('.locked').trigger('glue-movestart');
				key_moving = true;
			}
			// prevent window scrolling
			return false;
		} else if (e.ctrlKey && e.which == 65) {
			// select all objects not locked objects
			// selected locked objects will be unselected
			$('.object').not('.glue-selected').not('.locked').each(function() {
				$.glue.sel.select($(this));
			});
			// exclude locked objects from selection
			$('.locked.glue-selected').each(function() {
				$.glue.sel.deselect($(this));
			});
			return false;
		} else if (e.ctrlKey && e.which == 68) {
			// select none
			$.glue.sel.none();
			return false;
		} else if (e.ctrlKey && e.which == 73) {
			// invert selection
			var next = $('.object').not('.glue-selected').not('.locked');
			$.glue.sel.none();
			$(next).each(function() {
				$.glue.sel.select($(this));
			});
			return false;
		} else {
			// DEBUG
			//console.log('html keydown '+e.which);
		}
	});
	
	$('html').bind('keyup', function(e) {
		if (33 == e.which && e.shiftKey && $('.glue-selected').length) {
			// shift+pageup: move objects to top of stack
			$('.glue-selected').not('.locked').each(function() {
				$.glue.stack.to_top($(this));
				$.glue.object.save($(this));
			});
			$.glue.stack.compress();
			return false;
		} else if (34 == e.which && e.shiftKey && $('.glue-selected').length) {
			// shift+pagedown: move objects to bottom of stack
			$('.glue-selected').not('.locked').each(function() {
				$.glue.stack.to_bottom($(this));
				$.glue.object.save($(this));
			});
			$.glue.stack.compress();
			return false;
		} else if (37 <= e.which && e.which <= 40 && $('.glue-selected').length) {
			// move selected elements with arrow keys
			$('.glue-selected').not('.locked').trigger('glue-movestop');
			key_moving = false;
			return false;
		} else if (e.which == 46 && $('.glue-selected').length) {
			// delete selected objects
			// this is pretty much copied from object-edit.js
			var objs = $('.glue-selected').not('.locked');
			$(objs).each(function() {
				var id = $(this).attr('id');
				$.glue.object.unregister($(this));
				$(this).remove();
				// delete in backend as well
				$.glue.backend({ method: 'glue.delete_object', name: id });
				// update canvas
				$.glue.canvas.update();
			});
			return false;
		} else {
			// DEBUG
			//console.log('html keydown '+e.which);
		}
	});
	
	// note: drag/resize (including axis-constrain, grid-snap, and
	// multi-select sync) are wired up per-object in $.glue.object.register()
	// via Moveable's dragStart/drag/dragEnd instead of delegated handlers
	// here, since Moveable doesn't emit jQuery-style 'drag'/'dragstart' DOM
	// events the way jQuery UI's draggable() did

	$('.object').live('click', function(e) {
		// TODO (later): moving objects after shift clicking on them does not seem to work right on Chrome, document and fill a bug upstream
		if (!e.shiftKey && !$(this).hasClass('glue-selected')) {
			$.glue.sel.none();
		}
		if (e.shiftKey && $(this).hasClass('glue-selected')) {
			$.glue.sel.deselect($(this));
		} 
		// shift clicking involving locked object will result in no action
		else if (e.shiftKey && $(this).hasClass('locked') || $('.glue-selected').hasClass('locked')) {
			return;
		} else {
			$.glue.sel.select($(this));
		}

	});
	
	$('.object').live('glue-movestop', function(e) {
		// update tooltip
		$.glue.object.resizable_update_tooltip(this);
		// save object
		$.glue.object.save(this);
		// update canvas
		$.glue.canvas.update(this);
	});
	
	$('.object').live('glue-unregister', function(e) {
		$.glue.sel.deselect($(this));
	});
	
	return {
		// deselect an object
		// obj .. element
		deselect: function(obj) {
			if ($(obj).hasClass('glue-selected')) {
				var border = $(obj).outerHeight()-$(obj).innerHeight();
				$(obj).removeClass('glue-selected');
				$(obj).trigger('glue-deselect');
				var p = $(obj).position();
				$(obj).css('left', (p.left+border/2)+'px');
				$(obj).css('top', (p.top+border/2)+'px');
				//$(obj).css('width', ($(obj).width()+border)+'px');
				//$(obj).css('height', ($(obj).height()+border)+'px');
				// DEBUG
				//console.log('deselected '+$(obj).attr('id'));
			}
		},
		// select none
		none: function() {
			$('.glue-selected').each(function() {
				$.glue.sel.deselect($(this));
			});
		},
		// select an object
		// obj .. element
		select: function(obj) {
			// TODO (later): handle more than one obj (and change callers)
			if (!$(obj).hasClass('glue-selected')) {
				$(obj).addClass('glue-selected');
				$(obj).trigger('glue-select');
				// TODO (later): the following code works for dashed borders but 
				// not for solid ones - read out the border-style on the fly and 
				// act accordingly (there seem to be a problem with getting the 
				// information through jQuery 1.4.3 however)
				// also needs changes above and in register_alter_pre_save
				var p = $(obj).position();
				var border = $(obj).outerHeight()-$(obj).innerHeight();
				$(obj).css('left', (p.left-border/2)+'px');
				$(obj).css('top', (p.top-border/2)+'px');
				//$(obj).css('width', ($(obj).width()-border)+'px');
				//$(obj).css('height', ($(obj).height()-border)+'px');
				// DEBUG
				//console.log('selected '+$(obj).attr('id'));
			}
		},
		// return if an object is selected
		// obj .. element
		selected: function(obj) {
			return $(obj).hasClass('glue-selected');
		}
	};
}();

$.glue.slider=function(){return function(d,g,a){var c=d;var b=function(h){if(typeof g=="function"){g(h.pageX-c.pageX,h.pageY-c.pageY,h)}return false};var f=function(h){$("html").unbind("mousemove",b);$("html").unbind("mouseup",f);if(typeof g=="function"){g(h.pageX-c.pageX,h.pageY-c.pageY,h)}if(typeof a=="function"){a(h.pageX-c.pageX,h.pageY-c.pageY,h)}return false};$("html").bind("mousemove",b);$("html").bind("mouseup",f)}}();$.glue.stack=function(){var a=100;var c=199;var d=0;var b=function(h,e){var j=$(h).outerHeight();var l=$(h).position();var g=$(h).outerWidth();var f=$(e).outerHeight();var i=$(e).position();var k=$(e).outerWidth();if((l.left<=i.left+k&&i.left<=l.left+g)&&(l.top<=i.top+f&&i.top<=l.top+j)){return true}else{return false}};return{compress:function(){var e=d-1;var h=c+1;var f=0;$(".object").not(".locked").each(function(){var i=parseInt($(this).css("z-index"));if(isNaN(i)){return}if(i<h){h=i}if(e<i){e=i}});for(var g=h;g<=e;g++){var j=false;$(".object").not(".locked").each(function(){var i=parseInt($(this).css("z-index"));if(isNaN(i)){return}else{if(i==g){j=true}}});if(!j){e--;$(".object").not(".locked").each(function(){var i=parseInt($(this).css("z-index"));if(isNaN(i)){return}else{if(g<i){$(this).css("z-index",--i);$(this).addClass("need_save")}}})}}f=a-Math.round((e-h)/2)-h;if(Math.abs(f)<20){f=0}else{$(".object").addClass("need_save")}$(".need_save").each(function(){var i=parseInt($(this).css("z-index"));if(!isNaN(i)){$(this).css("z-index",i+f);$.glue.object.save(this)}$(this).removeClass("need_save")})},default_z:function(){return a},to_bottom:function(f){var e=c+1;var g=parseInt($(f).css("z-index"));$(".object").not(".locked").each(function(){if(this==$(f).get(0)){return}if(!b(f,this)){return}else{}if($(this).css("z-index").length){var h=parseInt($(this).css("z-index"));if(!isNaN(h)&&h<e){e=h}}});if(isNaN(g)||e<=g){if(e<=c&&d<e){$(f).css("z-index",e-1);return true}}return false},to_top:function(f){var e=d-1;var g=parseInt($(f).css("z-index"));$(".object").not(".locked").each(function(){if(this==$(f).get(0)){return}if(!b(f,this)){return}else{}if($(this).css("z-index").length){var h=parseInt($(this).css("z-index"));if(!isNaN(h)&&e<h){e=h}}});if(isNaN(g)||g<=e){if(d<=e&&e<c){$(f).css("z-index",e+1);return true}}return false}}}();$.glue.upload=function(){var a=function(c,b){if(c===undefined){c=0}if(b===undefined){b=0}var d=0;return{error:function(f){d--;if(d==0){$(this.status).detach()}if(f&&f.target&&f.target.status){$.glue.error("There was a problem uploading a file (status "+f.target.status+")")}else{$.glue.error("There was a problem uploading a file. Make sure you are not exceeding the file size limits set in the server configuration.");console.error(f)}},finish:function(e){d--;if(d==0){$(this.status).detach()}$.glue.upload.handle_response(e,this.x,this.y)},progress:function(f){$(this.status).children(".glue-upload-statusbar-done").css("width",(f.loaded/f.total*100)+"%");$(this.status).attr("title",f.loaded+" of "+f.total+" bytes ("+(f.loaded/f.total*100).toFixed(1)+"%)")},start:function(f){$.glue.menu.hide();d++;$("body").append(this.status);$(this.status).children(".glue-upload-statusbar-done").css("width","0%");$(this.status).css("left",(this.x-$(this.status).outerWidth()/2)+"px");$(this.status).css("top",(this.y-$(this.status).outerHeight()/2)+"px")},status:$('<div class="glue-upload-statusbar glue-ui" style="position: absolute; z-index: 202;"><div class="glue-upload-statusbar-done"></div></div>'),x:c,y:b}};$(document).ready(function(){var c=$('<div style="height: 32px; max-height: 32px; max-width: 32px; overflow: hidden; width: 32px;"><img src="'+$.glue.base_url+'img/upload.png" alt="btn" width="32" height="32"></div>');var b=a();b.multiple=true;$.glue.upload.button(c,{method:"glue.upload_files",page:$.glue.page},b);$(c).bind("click",function(f){var d=$.glue.menu.spawn_coords();b.x=d.x;b.y=d.y});$.glue.menu.register("new",c,11);$("html").get(0).addEventListener("dragover",function(d){d.stopPropagation();d.preventDefault()},false);$("html").get(0).addEventListener("drop",function(f){f.stopPropagation();f.preventDefault();var d=a(f.pageX,f.pageY);$.glue.upload.files(f.dataTransfer.files,{method:"glue.upload_files",page:$.glue.page},d)},false)});return{button:function(c,d,b){if(!b){b={}}if(!b.tooltip){b.tooltip="upload a file"}$(c).prepend('<input type="file" title="'+b.tooltip+'" style="height: 100%; opacity: 0; position: absolute; width: 100%; z-index: 300;">');if(b.multiple){$(c).children("input").first().attr("multiple","multiple")}$(c).children("input").first().bind("change",function(f){if(!this.files||this.files.length==0){if(typeof b.abort=="function"){b.abort()}return false}else{$.glue.upload.files(this.files,d,b);return false}})},files:function(b,e,m){if(!e){e={}}if(!m){m={}}var l=new XMLHttpRequest();if(typeof m.progress=="function"){l.upload.onprogress=function(f){m.progress(f)}}if(typeof m.finish=="function"){l.onload=function(f){try{m.finish($.parseJSON(f.target.responseText))}catch(f){if(typeof m.error=="function"){m.error(f)}}}}if(typeof m.error=="function"){l.onerror=function(f){m.error(f)}}l.open("POST",$.glue.base_url+"json.php",true);if(window.FormData){var j=new FormData();for(var k in e){j.append(k,JSON.stringify(e[k]))}for(var g=0;g<b.length;g++){j.append("user_file"+g,b[g])}l.send(j);if(typeof m.start=="function"){m.start(b)}return true}else{$.glue.error("Your browser is not supported. Update to a recent version of Firefox or Chrome.");if(typeof m.abort=="function"){m.abort()}return false}},handle_response:function(f,k,j){if(!f){$.glue.error("There was a problem communicating with the server")}else{if(f["#error"]){$.glue.error("There was a problem uploading the file ("+f["#data"]+")")}else{if(f["#data"].length==0){$.glue.error("The server did not reply with any object. The file type you were uploading could either not be supported (look around for more modules!) or there could be an internal problem. Check the log file to be sure!");return}$.glue.sel.none();for(var g=0;g<f["#data"].length;g++){var e=$(f["#data"][g]);var l=function(o){var p=o.data.mode;var m=o.data.target_x;var i=o.data.target_y;if($(this).hasClass("object")){var n=$(this)}else{var n=$(this).parents(".object").first()}$(n).css("width",$(n).width()+"px");$(n).css("height",$(n).height()+"px");$(n).trigger("glue-upload-dynamic-late",[this]);if(p=="center"){$(n).css("left",(m-$(n).outerWidth()/2)+"px");$(n).css("top",(i-$(n).outerHeight()/2)+"px")}else{$(n).css("left",(m+"px"));$(n).css("top",(i+"px"))}$(n).css("visibility",$(n).data("orig_visibility"));$(n).removeData("orig_visibility");$.glue.object.register(n);$.glue.object.save(n)};if(f["#data"].length==1){var h="center";var d=k;var c=j}else{var h="stack";var d=k+g*$.glue.grid.x();var c=j+g*$.glue.grid.y()}var b=parseInt($(e).get(0).style.getPropertyValue("width"));if(isNaN(b)||b===0){$(e).bind("load",{mode:h,target_x:d,target_y:c},l);$(e).find("*").bind("load",{mode:h,target_x:d,target_y:c},l);$(e).data("orig_visibility",$(e).css("visibility"));$(e).css("visibility","hidden");$("body").append(e);$(e).trigger("glue-upload-dynamic-early",[h,d,c])}else{$("body").append(e);if(h=="center"){$(e).css("left",(d-$(e).outerWidth()/2)+"px");$(e).css("top",(c-$(e).outerHeight()/2)+"px")}else{$(e).css("left",(d+"px"));$(e).css("top",(c+"px"))}$.glue.object.register(e);$(e).trigger("glue-upload-static",[h]);$.glue.object.save(e)}}}}}}}();$(document).ready(function(){$(".object").each(function(){$.glue.object.register($(this))});$.glue.canvas.update();var b;$(window).bind("resize",function(c){clearTimeout(b);b=setTimeout(function(){$.glue.canvas.update()},100)});var a=false;$("html").bind("click",function(c){window.focus();if(c.target==$("body").get(0)){if(!$.glue.menu.is_shown()){if(a){clearTimeout(a);a=false;$.glue.menu.show("page",c.clientX,c.clientY);return false}a=setTimeout(function(){a=false;if($.glue.menu.prev_menu()==""){$.glue.menu.show("new",c.clientX,c.clientY)}},300)}}});$("html").bind("keyup",function(c){if(c.altKey&&c.which==79){$.glue.menu.show("new");return false}else{if(c.altKey&&c.which==80){$.glue.menu.show("page");return false}else{if(c.ctrlKey&&c.which==90){if(confirm('Looking for an "undo" option?\nHOTGLUE keeps record of your recent edits - it\'s called "revisions".\nWould you like to browse through the revisions of this page?')){window.location=$.glue.base_url+"?"+$.glue.page+"/revisions";return false}}}}});$("html").bind("mousedown",function(c){if($(c.target).closest(".moveable-control, .moveable-line").length){return}return false})});